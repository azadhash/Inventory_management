<!DOCTYPE html>
<html>
  <head>
    <title>InventoryManagement</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="csrf-token" content="<%= form_authenticity_token %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'notification', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/e79d88cb25.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
    <%= javascript_include_tag 'https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js' %>
    <%= javascript_include_tag 'delete' %>
    <style>
    .notification-dropdown {
      min-width: 300px;
    }
    </style>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark custom-bg-color">
      <div class="container">
        <a class="navbar-brand" href="#">InventoryManagement</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <% if current_user %>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <% if authenticate_user %>
              <li class="nav-item">
                <%= link_to "Dashboard", dashboard_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item">
                <%= link_to "Users", users_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item">
                <%= link_to "Brands", brands_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item">
                <%= link_to "Category", categories_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item">
                <%= link_to "Items", items_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item">
                <%= link_to "Issues", issues_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item">
                <%= link_to "Storage", storage_categories_path, class: 'nav-link text-light' %>
              </li>
              <% else %>
              <li class="nav-item">
                <%= link_to "Items", items_path, class: 'nav-link text-light' %>
              </li>
              <li class="nav-item">
                <%= link_to "Issues", issues_path, class: 'nav-link text-light' %>
              </li>
              <% end %>
              <li class="nav-item">
                <%= link_to "Logout", logout_path, class: 'nav-link text-light' %>
              </li>
            </ul>
          </div>
          </div>
          <div class="dropdown">
          <a class="btn dropdown-toggle text-light" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-regular fa-bell text-light"></i>
            <span class="badge bg-danger" id="notificationCounter"><%= current_user.notifications.where(read: false).count %></span>
          </a>
          <ul class="dropdown-menu notification-dropdown dropdown-menu-end" aria-labelledby="dropdownMenuLink" id="notification">
            <li><%= button_to 'Mark All as Read', notifications_mark_read_path, remote: true, method: :patch, class: 'text-light btn custom-bg-color mb-3', id: 'markAllReadButton' %></li>
            <% current_user.notifications.where(read: false).each do |notification| %>
              <% priority_class = '' %>
              <% case notification.priority %>
                <% when 'warning' %>
                  <% priority_class = 'text-warning' %>
                <% when 'danger' %>
                  <% priority_class = 'text-danger' %>
                <% else %>
                  <% priority_class = 'text-info' %>
              <% end %>
              <li class="notification-item <%= priority_class %>"><%= notification.message %></li>
            <% end %>
          </ul>
          </div>
          <%if authenticate_user %>
            <span class="navbar-text text-light"> <%= current_user.name.capitalize %> Admin </span>
          <% else %>
            <span class="navbar-text text-light"> <%= current_user.name.capitalize %> Employee </span>
          <% end %>
      <% end %>
    </nav>
    <div class="container">
      <div id="flash-messages">
        <%= render 'partials/flash'%>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
